export { GET, POST } from '@auth';

/*
    The name [...nextauth] is just a convention, 
    The important part is the [...] — that's Next.js syntax for a catch-all route. 
    It means "match any URL segment after /api/auth/".
    So [...nextauth] = catch-all route, named nextauth just to make it obvious what it's for.
*/

